{
  "name": "comote",
  "version": "0.2.4",
  "private": true,
  "type": "module",
  "description": "Comote local remote companion for Codex Desktop.",
  "scripts": {
    "app:build": "node scripts/build-macos-app.mjs",
    "desktop:icons": "node scripts/generate-desktop-assets.mjs",
    "desktop:dev": "npm run desktop:icons && npm run sidecar:build && tauri dev",
    "dist:mac": "npm run desktop:icons && npm run sidecar:build:mac && node scripts/install-prod-deps.mjs && tauri build --bundles app --target aarch64-apple-darwin && node scripts/create-mac-dmg.mjs arm64",
    "dist:mac:x64": "npm run desktop:icons && npm run sidecar:build:mac:x64 && node scripts/install-prod-deps.mjs && tauri build --bundles app --target x86_64-apple-darwin && node scripts/create-mac-dmg.mjs x64",
    "dist:mac:all": "npm run dist:mac && npm run dist:mac:x64",
    "dist:win": "npm run desktop:icons && npm run sidecar:build:win && node scripts/install-prod-deps.mjs && tauri build --bundles nsis --target x86_64-pc-windows-msvc && node scripts/collect-tauri-artifacts.mjs win",
    "sidecar:build": "node scripts/build-sidecar.mjs current",
    "sidecar:build:mac": "node scripts/build-sidecar.mjs aarch64-apple-darwin",
    "sidecar:build:mac:x64": "node scripts/build-sidecar.mjs x86_64-apple-darwin",
    "sidecar:build:win": "node scripts/build-sidecar.mjs x86_64-pc-windows-msvc",
    "dev": "node src/server/index.js",
    "wechat:check": "node scripts/check-wechat-channel.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.56.1",
    "qrcode-generator": "^1.4.4"
  },
  "overrides": {
    "axios": "1.16.1"
  }
}
